✅ Python Project 2: To-Do List (CLI) 📝

📌 Problem Statement:  
Create a command-line To-Do List app that lets users add, view, and delete tasks during a session.

:

🛠 Step-by-Step Plan
1. Plan Features

2. Add Task → user types a task and it goes into a list.

3. View Tasks → show all tasks with numbers.

4. Delete Task → remove a task by its number.

5. Exit → quit the program.

6. Use a List to Store Tasks

Example: tasks = []

Every time user adds a task, append it to the list.

When they delete, remove by index.